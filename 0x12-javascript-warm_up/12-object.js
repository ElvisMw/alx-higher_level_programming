#!/usr/bin/node
const myObject = {
    type: 'object',
    value: 12
  };
  console.log(myObject);
  
  myObject.value = 89; // Changing the value property from 12 to 89
  
  console.log(myObject);
