#!/usr/bin/python3
# This function returns the number
# of keys in a dictionary.
# The prototype is 'def number_keys(a_dictionary):'
# Not allowed to import any module


def number_keys(a_dictionary):
    _numero_keys = len(a_dictionary)
    return _numero_keys
